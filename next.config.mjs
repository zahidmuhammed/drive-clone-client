/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "lh3.googleusercontent.com",
            "driveproject.s3.ap-south-1.amazonaws.com",
        ],
    },
};

export default nextConfig;
